import React from "react";
import "./Style.css";
export const Buttons = ({ title }) => {
  return <button className="btn">{title}</button>;
};

export const Buttonss = ({ title }) => {
  return <button className="btn2">{title}</button>;
};
